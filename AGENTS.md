# Verification scope

For nonvisual sitemap, service worker routing, SEO asset, and build configuration changes, verify the affected behavior and build output. Skip page layout, responsive screenshot, and control spacing checks unless the rendered interface changes or the user requests a UI review. Keep checks proportional to the task.
